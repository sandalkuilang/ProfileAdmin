SELECT VALUE, TEXT FROM (
	SELECT '' VALUE,
			'Any Value' Text
	UNION ALL
	SELECT DISTINCT(NETWORK_ID) AS Value,
		NETWORK_ID AS Text
	FROM USER_PROFILE 
	WHERE APPLICATION = @Application
) X 
ORDER BY VALUE